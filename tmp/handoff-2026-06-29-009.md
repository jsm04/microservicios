# Handoff — Task 009: Fix auth.model.ts `db` reference bug

## Status
Task **not started**. Next agent should execute it.

## What was done this session
1. Indexed codebase (427 nodes, 628 edges)
2. Discovered critical bugs in `services/auth/models/auth.model.ts`
3. Created task file: `docs/tasks/009-fix-auth-model-db-reference.md`
4. Updated HARNESS.md and session.md

## What needs to be done next
Execute **Task 009**: Fix the auth model bugs identified during graph analysis.

### The bug
`services/auth/models/auth.model.ts` defines `getDb()` (returns `_db` or new Pool) but 3 functions use an undefined variable `db` instead:

| Function | Line | Current code | Should be |
|----------|------|-------------|-----------|
| `findUserByEmail` | ~65 | `await db.query(...)` | `await getDb().query(...)` |
| `findUserById` | ~74 | `await db.query(...)` | `await getDb().query(...)` |
| `emailExists` | ~83 | `await db.query(...)` | `await getDb().query(...)` |

Additionally, `setTestPool` and `getDb` are **duplicated** (lines 16-24 and 26-34 — copy-paste artifact). Remove the duplicate block.

### Impact
Login, user lookup by email, user lookup by ID, and email-check all crash at runtime with `ReferenceError: db is not defined`. Only `createUser` works correctly (it uses `getDb()`).

### Steps to execute
1. Read `services/auth/models/auth.model.ts`
2. Replace `db.query` → `getDb().query` in the 3 affected functions
3. Remove duplicate function block
4. Run: `bun test services/auth/auth.test.ts` — verify all tests pass
5. Update task status to "completed" in HARNESS.md and session.md
6. Clear lock in `tmp/lock.md`

### Additional finding (separate task)
Port mismatch in docs: `docs/architecture.md` and `CONTEXT.md` say Auth runs on port 3000, but `docker-compose.yml` has it on **3003**. Not urgent — just stale docs.

## Suggested skills
- **karpathy-guidelines** — for careful surgical code changes
- **systematic-debugging** — if any test fails unexpectedly
